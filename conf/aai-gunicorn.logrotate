# logrotate configuration
# installs in /etc/logrotate.d/gunicorn
#
# TODO file is set in AAI/Dockerfile.gunicorn, instead use aai.py --loghandler rotating there?
# Note: mounts in logs/nginx not gunicorn

/opt/starbug.com/logs/nginx/aai-gunicorn-access.log /opt/starbug.com/logs/nginx/aai-gunicorn-error.log {
    dateext
    weekly
    rotate 8
    missingok
    notifempty
    copytruncate
    compress
}
